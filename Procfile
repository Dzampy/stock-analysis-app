web: gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --threads 2 --timeout 120
# Alternative entry point using run.py (for refactored structure):
# web: gunicorn run:app --bind 0.0.0.0:$PORT --workers 2 --threads 2 --timeout 120

